# Plasma

```bash
sudo pacman -S plasma plasma-meta plasma-desktop 
sudo pacman -S sddm sddm-kcm
systemctl enable sddm.service

# Optional
sudo pacman -S kde-applications-meta kde-applications pacman -S plasma-wayland-session

```
